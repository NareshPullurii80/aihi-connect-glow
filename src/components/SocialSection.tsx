// This component is now integrated into ContactForm with fixed positioning
// Keeping this file for potential future use
const SocialSection = () => {
  return null;
};

export default SocialSection;